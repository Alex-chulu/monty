#include "monty.h"
/**
 * f_stack - prints the top
 * @head: head of stack
 * @counter: line counter
 * Return: no
 */

void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
